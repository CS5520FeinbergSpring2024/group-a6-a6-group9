<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    android:background="@drawable/sticker_picker">

    <Button
        android:id="@+id/receivedStickersButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Received Stickers"
        android:onClick="viewMessageHistory"/>

    <Button
        android:id="@+id/stickersCountButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Stickers Count"
        android:onClick="viewStickersCount"/>

</LinearLayout>
